apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: infra-nfs-pvc
 namespace: default
spec:
 storageClassName: "ssd"
 accessModes:
   - ReadWriteMany
 resources:
   requests:
     storage: 1Ti
 selector:
   matchLabels:
     type: nfs-infra-storage
